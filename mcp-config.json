{
  "mcpServers": {
    "zeldaret-mm": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-v", "${PWD}:/mm",
        "-w", "/mm",
        "ghcr.io/zeldaret/mm",
        "bash"
      ]
    }
  }
}
